- **UI/UX Typography Standards**:
  - Secondary labels, metadata tags, and small utility buttons must strictly use `text-[8px] font-bold uppercase tracking-wider`.
  - Internal component titles, standard text, and form labels must strictly use `text-[10px] font-bold` or `text-[10px]`.
  - Main page titles and hero components must strictly use `text-[8px] md:text-[10px] font-bold`.
  - Legacy sizes (`text-xs`, `text-sm`, `text-base`, `text-lg`, `text-xl`, etc.) and odd sizes (`text-[7px]`, `text-[9px]`, `text-[11px]`) are strictly prohibited to maintain the extreme high-density technical aesthetic. Maintain the `ui.md` tracker concurrently with any style pass.

- Phase 61 'Connectivity Intelligence & Security Hardening' is completed, featuring a new `diagnoseConnection` utility in `src/lib/gcp/storage-validator.ts` for multi-layer troubleshooting (DNS, TCP, Service Auth) and a corresponding UI troubleshooting modal in `StorageSection.tsx` that displays step-by-step progress and actionable recommendations.
- Ensure TypeScript interfaces are fully leveraged to avoid unsafe `any` assertions, as demonstrated by the `drifted` property fix on the `Migration` interface in `src/lib/gcp/migrations.ts`.
- Phase 64 'Connectivity Security Hardening & CLI Diagnostic Parity' is completed, featuring a new `deployify storage diagnose` CLI command, SSL enforcement toggles for database connectors, and centralized Cloud SQL Auth Proxy orchestration (v2.11.0) in `src/lib/gcp/cloudsql.ts`.
- Phase 65 'IP Visibility & Diagnostic Intelligence' is completed, adding a regional egress IP utility in `src/lib/gcp/networks.ts` and an 'IP Allowlist Assistant' in the Storage Usage Guide UI to assist in configuring firewalls for external database providers based on the project's GCP region.
- **Operational Integrity**: The platform has been certified to maintain 100% operational integrity across all core metrics. All code modifications must strictly preserve passing states for `npm run test`, `npm run lint`, `npm run build`, and `MOCK_DB=true npm run audit`. Ensure that any new functionality introduced maintains or improves this established operational baseline.
- Phase 66 'Managed Data Portability & Import/Export Orchestration' is completed, implementing native Cloud SQL import/export logic in `src/lib/gcp/cloudsql.ts` and a dedicated `DataPortabilityModal` for dashboard-driven data migration via Google Cloud Storage.
- When using `mysql2/promise` with a configuration variable that can be either a string URI or a `ConnectionOptions` object, use type narrowing (e.g., `typeof config === 'string' ? await mysql.createConnection(config) : await mysql.createConnection(config)`) before calling `createConnection()` to prevent TypeScript overload resolution errors.
- Phase 102 'Automated Read-Replica Orchestration & Global Traffic Steering' is completed, enabling horizontal scaling for Cloud SQL via the `/replica` API and intelligence-driven suggestions for read-heavy workloads.
- Strict Code Quality: Maintain a 100% warning-free codebase. Any new code must have zero `@typescript-eslint/no-unused-vars` and other ESLint issues to uphold maximum operational integrity.
- Ensure all new features are thoroughly documented in both .agent/IMPROVEMENTS.md and .agent/RULES.md after implementation.
- Always run MOCK_DB=true npm run audit before submitting code.
- MOCK_DB=true is strictly required when running the pre-launch audit to bypass active Firestore connections and test solely the environment/API route health.
- Phase 124 'Granular Secret Governance & Automated IAM Scoping' is completed, implementing resource-level least-privilege for GCP Secret Manager access via `grantSecretAccess` in `src/lib/gcp/secrets.ts`. The platform now detects and remediates broad project-level secret accessor roles, restricting service account access to only the specific credentials required by each storage connector.
- Phase 125 'Advanced Security Perimeter Hardening & Migration Resilience' is completed. It implements automated remediation for `public_ip_exposed`, `password_auth_used` (IAM auth migration), and `regional_mismatch` in `src/lib/gcp/remediation-utils.ts` utilizing existing operations (`updateInstanceSettings` and `migrateInstanceToRegion`) from `src/lib/gcp/cloudsql.ts`.
- Phase 137 'Telemetry-Driven Autonomous Indexing & Schema Optimization Intelligence' is completed. It introduced automated query impact scoring (Latency x Frequency) and missing index detection via execution plan analysis (using `analyzePlanForIndexes` in `src/lib/gcp/monitoring.ts`), surfaced through a new project-scoped Schema Optimization API and a high-density 'Index Advisor' in the Optimization Modal.
- Phase 141 'Autonomous Database Health Remediation & Intelligent Connection Leak Detection' is completed. It implemented `detectConnectionLeaks` in `src/lib/gcp/monitoring.ts` to identify stale database connections and `terminateIdleSessions` in `src/lib/gcp/cloudsql.ts` for automated recovery, orchestrated via a new Health Remediation API and surfaced through pulsating 'CONNECTION LEAK' badges in the high-density Storage UI.
- Phase 147 'Autonomous Firewall Verification & Connectivity Intelligence' is completed. It introduced `verifyFirewallReachability` in `src/lib/gcp/storage-validator.ts` to confirm regional egress IP allowlisting, integrated it into the diagnostic engine, and added a pulsating 'FIREWALL VERIFIED' badge in `StorageSection.tsx` driven by persisted diagnostic metadata.
- Phase 151 'Autonomous Database Maintenance - Intelligent Vacuuming & Statistics Optimization' is completed. It introduced `discoverStatisticsDrift` in `src/lib/gcp/monitoring.ts` to identify dead tuples (Postgres) and stale statistics (MySQL), integrated into the 24-hour sync heartbeat, and surfaced via 'STATISTICS DRIFT' badges and a 'Maintenance Advisor' in the UI.

- Session 247 'Platform Verification & Typographic Adherence' is completed. It successfully verified that the platform is completely stripped of legacy and odd-sized typography variants (e.g., `text-[9px]`, `text-[11px]`, `text-xs`, `text-sm`) and strictly complies with the Session 203 high-density technical visual aesthetic across all UI components. 100% operational integrity has been confirmed across full test suites, automated builds, and Pre-Launch Audits.
- Phase 145 'Autonomous Database Query Caching Intelligence & Automated Redis Orchestration' is completed. It introduced `detectCachingOpportunities` in `src/lib/gcp/monitoring.ts` to identify caching candidates via telemetry, surfaced through a new Caching Optimization API and a high-density 'Cache Advisor' in the Optimization Modal. The autonomous optimization workflow now supports automated GitHub PRs for caching implementations.

- **Typing and Code Quality Standards**:
  - Avoid using `any` (or `as any`) in type declarations, including within test files (e.g., `security-intelligence.test.ts`). Use explicitly defined types like `StorageConfig` or `LogEntry[]` to maintain strict typing.
  - Consistently clean up unused variables and imports in React components (e.g., `StorageSection.tsx`) to uphold the zero-warnings policy.
